name = "nscs-sched"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Direct deploy (alternative to Git-connected Pages):
#   SUPABASE_URL=... SUPABASE_ANON_KEY=... npx wrangler pages deploy dist --project-name=nscs-sched
#
# Git-connected deploy is preferred — see docs/DEPLOY.md
