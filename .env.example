# Copy to .env for local production builds (do not commit .env)
SUPABASE_URL=https://bnrlcpemxjkisnejqcbq.supabase.co
SUPABASE_ANON_KEY=eyJ...

# Service role key — setup/seed only, never commit or use in frontend build
# SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Database password — for npm run setup:supabase migration step only
# SUPABASE_DB_PASSWORD=

# Set to 1 for test builds (in-memory storage, no login gate)
# NSCS_TEST_MODE=1
